<%= simple_form_for(@company) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.input :cnpj %>
    <%= f.input :capital %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
